# Deployment Status

Verified on 2026-07-15.

- `npm ci`: passed
- `npm run build`: passed
- TypeScript: passed as part of the production build
- Routes verified at build time: `/`, `/manifest.webmanifest`, `/api/openapi`, `/api/proxy`
- Production target: Vercel
- Repository: `TheGamingBaer/Spedvhandyapp`

The SPEDV API key is not committed and is entered only in the installed PWA.
